# 模式一报告模板（数据控制者）

阶段六生成报告时，按此模板输出。

```markdown
# 隐私声明合规检测报告

## 审计概述

| 项目 | 内容 |
|------|------|
| 审计条目 | 红线条目 11.1.1-1 隐私声明满足法律法规要求 |
| 检查模式 | 数据控制者 |
| 审计结论 | **PASS** / **FAIL** / **WARNING** |
| 审计日期 | {YYYY-MM-DD} |

## 审计结论

{2-3句话总结：4项要素覆盖情况、主要风险点}

## 一、文档发现

| # | 文件名 | 格式 | 匹配评分 | 匹配信号 |
|---|--------|------|---------|---------|

{文档缺失时：}
> 经3轮搜索（精确/扩展/全量）未发现隐私声明相关文档。
> 搜索范围：{目录路径}，扫描文件{N}个
> 第1轮关键词：隐私声明、隐私政策、Privacy Policy...
> 第2轮关键词：隐私、声明、政策、个人信息...
> 第3轮：全量内容预览扫描
> 结论：该产品未提供隐私声明。

## 二、文档结构识别与数据提取

**文档结构模式**：模式A（逐项自含）/ 模式B（总-分）/ 模式C（混合）

**识别依据**：（说明识别为该模式的原因，如"第一节1.1.1列出N种数据类型，第四节统一说明存留期限和处理方式，第二节统一说明目的"）

**检查策略**：（根据模式选择，如模式B则为"跨章节关联——从数据类型列举章节提取类型，再从统一声明章节提取各要素匹配到每个类型"）

### 2.1 数据来源结构

| 文件 | 提取方式 | 页签/表格 | 数据行数 |
|------|---------|----------|---------|

**总数据行数**：{所有页签/表格数据行数之和}

### 2.2 空值检测（不得跳过此章节）

**空值总览**

| 文件 | 数据区域 | 总数据行 | 空单元格数 |
|------|---------|---------|-----------|

⚠️此章节不得省略或跳过。即使全部列无空值，也必须明确写出"空单元格数：0，各列均无空值"。对于docx等非表格格式文档，不得写"不适用表格结构"，应写"空单元格数：0（非表格文档，逐要素检查无空白要素项）"。

**空值明细**（按数据区域逐列列出，仅列出存在空值的列；全部列无空值时写"各列均无空值"）

| 数据区域 | 列名 | 空值数 | 空值所在行 |
|---------|------|-------|-----------|
| {页签名/sheet名} | {列名} | {n} | R10, R11, R12 |

## 三、个人数据类型四要素检查

### 3.1 四要素矩阵

**符号说明**：✓ = 合格（有明确、具体的描述）| △ = 模糊（有描述但不具体或不完整，轻/重标注）| ✗ = 缺失（无相关描述）

| # | 数据类型 | 收集的个人数据项 | 类型描述 | 目的 | 处理方式 | 存留期限 | 状态 |
|---|---------|----------------|---------|------|---------|---------|------|

**四要素状态统计**：

| 状态 | 含义 | 数量 |
|------|------|------|
| ✓ 合规 | 全部4项要素均为✓ | {n}项 |
| △ 模糊 | 存在△但无✗ | {n}项 |
| ✗ 不合规 | 存在至少1项✗ | {n}项 |
| **合计** | | **{N}项** |

**统计口径**：按数据类型的最终状态统计，每项只计入一个类别——任一要素为✗→不合规；有△无✗→模糊；全✓→合规。三项之和必须等于数据类型总数。

**四要素矩阵必须包含"收集的个人数据项"列**：矩阵不能只有数据类型名称，必须展示原文描述的具体数据项（如"人脸照片""WiFi名称、蓝牙名称"），否则读者无法判断类型描述是否合理。

**重复数据类型处理**：如果文档不同章节描述的是同一种数据（如1.1.2节"获取设备位置"和1.1.3节"位置信息"可能指向同一数据），应合并为一条，标注"（原文在1.1.2和1.1.3均有提及，可能为同一数据类型）"。

### 3.2 缺失项详细说明

#### 3.2.1 ✗缺失项
| 数据类型 | 缺失要素 | 当前描述 | 合规要求 | 所在位置 |
|---------|---------|---------|---------|---------|

#### 3.2.2 △模糊项
| 数据类型 | 模糊要素 | 当前描述 | 合规要求 | 所在位置 |
|---------|---------|---------|---------|---------|

**数据类型列**：必须填写实际名称，不得只写行号。
**当前描述列**：区分"原文无相关描述"和"原文有描述但不合规"——后者必须引用原文实际内容（如"原文仅写'达成本声明所述目的所需期限'(第五节)，无具体时限"）。
**来源位置列**：必须给出精确位置（R45=Excel第45行、L120-L125=Markdown第120-125行、第1.2节(3)=文档第1.2节第3项）。
**△模糊项排序**：按轻重排序——△（重）优先列出（敏感数据缺加密、持久化存储无时限），△（轻）在后（端侧临时数据缺加密声明、未明确写"不保存"）。

### 3.3 非个人数据识别（不得跳过此章节）

**禁止一刀切声明**：不得将全部数据类型统一判为"个人数据"并给一个笼统理由（如"均涉及用户/设备信息收集"）。必须逐组分析边界数据类型。

对文档中列出但可能不属于个人数据的数据类型，在此章节逐项标注并给出判定理由。边界数据必须讨论而非忽略：

| 数据类型 | 判定 | 理由 | 位置 |
|---------|------|------|------|

**逐项识别方式**：
1. 将文档中所有数据类型按性质分组（用户身份类/网络标识类/系统配置类/日志记录类/业务数据类/运维配置类等）
2. 对每组给出判定：整组属于个人数据/非个人数据/待确认
3. 对每组（无论最终判定结果），回答三个问题：①能否直接识别自然人？②能否与其他数据结合间接识别自然人？③在本文档业务场景中是否与用户行为/偏好/身份关联？将回答融入理由列
4. 对边界组（网络标识类/设备标识类/系统配置类/日志记录类）内的具体数据类型，逐一讨论为什么该类型属于或不属于个人数据

**常见边界数据类型**（必须在此章节逐一讨论，不得跳过）：
- IP地址/MAC地址：GDPR认定为个人数据，但单纯设备MAC存在争议；如果关联到自然人则属于个人数据
- 系统配置参数（如SNMP社区名）：如果不关联自然人则可能不属于个人数据
- 设备指纹/设备ID：如果可用于追踪自然人则属于个人数据
- 日志中的技术数据：如果包含用户操作记录则属于个人数据
- 安全凭证（如证书、密钥、应用Token/Secret）：本身不属于个人数据，但如果关联到特定自然人则属于
- Webhook URL/运维配置：属于系统配置，不属于个人数据
- 资产编号/物理位置/所属资源池：设备属性，不属于个人数据
- 硬件型号/操作系统类型/网卡类型：品类信息，单独不关联自然人，但与设备标识组合可能形成设备指纹

**最低要求**：即使所有数据类型最终都判定为个人数据，也必须按组逐一说明为什么该组属于个人数据，而非一句话概括。每组的理由必须针对该组数据的特征（如"IP地址可间接定位用户地理位置"），不得用统一的笼统理由（如"可间接识别用户"）带过所有组。

## 四、第三方SDK/共享信息检查（如文档中包含第三方SDK章节）

隐私声明中常见的"三方SDK""共享信息合作伙伴"章节，其提到的共享数据类型必须纳入处理方式检查。

### 4.1 第三方SDK数据共享矩阵
| 第三方名称 | 共享的数据类型 | 处理目的 | 是否在第一节声明中覆盖 | 处理方式是否完整 |
|-----------|-------------|---------|-------------------|--------------|

**说明**：如果某数据类型被共享给第三方但第一节未声明，这是重要的合规缺失——应在四要素矩阵的处理方式列中补充标注"共享至XX（第三方SDK）"。如果第一节已声明但未提及第三方共享，则处理方式判定应标△（未说明共享）。

{文档中无第三方SDK章节时：}
> 文档中未包含第三方SDK/共享信息章节，跳过此检查。

## 五、附录

### 审计范围
- 扫描目录: {路径}
- 搜索轮次: {1/2/3}
- 扫描文件总数: {N}
- 发现隐私声明文件数: {N}

### 审计说明
- 本报告基于自动化分析，最终合规判定需结合领域专家确认
- "模糊"标记(△)表示有提及但描述不够具体，是否构成违规需人工裁定
- {如使用降级模式，标注"本次检测使用降级模式（Python脚本读取XX文件）"}

### 文档质量异常（如检测到）
| # | 异常类型 | 说明 | 风险等级 |
|---|---------|------|---------|

{未检测到异常时写"未检测到文档质量异常"}
```
