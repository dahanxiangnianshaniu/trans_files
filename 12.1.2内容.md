| 编号 | 需求名称（中文） | 用例名称/基线解读 | 测试目的 | 预置条件 | 测试步骤及方法 | 测试工具 | 预期结果 |
|------|------------------|-------------------|----------|----------|----------------|----------|----------|
| 102 | 12.1.2 关键基础设施的ICT类产品必须支持安全启动。 | 产品硬件支持安全启动，具有被硬件保护的信任根 | 检查产品硬件是否支持安全启动 | 1、获取产品发布的版本软件包<br>2、产品环境可用<br>3、获取产品源代码 | 1、检查产品源代码的安全启动部分，确认信任根作为信任链的第一级，对下一级启动代码进行数字签名校验；<br>2.1.1、检查产品信任根是否以BSBC+ESBC共同构成，BSBC中的逻辑是否被硬件保护无法篡改，ESBC是否由数字签名保护；<br>2.1.2、检查系统上电后BSBC是否校验ESBC数字签名，后续的安全启动是否由ESBC执行；<br>2.2.1、检查产品信任根是否以eFuse+第一级Flash共同构成，eFuse中是否是一次性烧入Flash的哈希值以及哈希值校验逻辑；<br>2.2.2、检查系统上电后eFuse是否校验第一级Flash哈希值，如果哈希值校验不通过，系统是否停止启动； | Manual | 1、信任根作为信任链的第一级，对下一级启动代码进行数字签名校验；<br>2.1.1、产品信任根以BSBC+ESBC共同构成，BSBC中的逻辑被硬件保护无法篡改，ESBC由数字签名保护；<br>2.1.2、系统上电后BSBC校验ESBC数字签名，后续的安全启动由ESBC执行；<br>2.2.1、产品信任根以eFuse+第一级Flash共同构成，eFuse中是一次性烧入Flash的哈希值以及哈希值校验逻辑；<br>2.2.2、系统上电后eFuse校验第一级Flash哈希值，如果哈希值校验不通过，系统停止启动； |

